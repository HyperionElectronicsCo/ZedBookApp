# ZedBook

AIDE/Gradle Android project.

Source is kept only in:

`app/src/main/java`

Resources are kept only in:

`app/src/main/res`

No duplicate root `src/`, `res/`, `gen/`, or root `AndroidManifest.xml` folders are included.
